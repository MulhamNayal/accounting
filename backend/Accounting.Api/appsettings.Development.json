{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Seed": {
    "//": "Local convenience only. This file is committed to a public repository, so a deployed instance must override Seed__DemoPassword rather than inherit this. An unset value means no seeding at all.",
    "DemoPassword": "accounting-demo"
  }
}
